#include <stdio.h>

int main ()
{
    // Declaring Variables
    int a = 2;
    float b = 7.5;
    int sum = a+b;
    char c = 'u';

    printf("The value of a : %d \n", a);
    printf("The value of b : %f \n", b);
    printf("The character value of c : %c \n" , c);

    // now printing the sum of a and b
    printf("The Sum of a+b : %f \n", a+b);
    printf("The sum of a+b: %f \n", sum);

    printf("learning c basics");
    return 0;
}