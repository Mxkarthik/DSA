#include <iostream>
#include <vector>
using namespace std;

void multiplication (vector <int> array1 , vector <int> array2 , int size1 ,int size2)
{

    if ( m != y) 
    {
        cout<<"The matrix multiplication is not possible";
    }
    int answer = 0;
    for (int i = 0 ; i <  ;i++)
    {
        for (int j = 0 ; j < size ; j++)
        {
            answer 
        }
    }
}

int main ()
{
    int m ; int n; int size1;
    int x ; int y; int size2;

    vector <int> array1 (size1);
    cout<<"Please Enter the values for Matrices 1";
    for (int i = 0 ; i < m ; i++)
    {
        for (int j = 0 ; j < n ; j++)
        {
            cin>>array[i] [j];
        }
    }
    vector <int> array2 (size2);
    cout<<"Please Enter the values for Matrices 2";
    for (int i = 0 ; i < x ; i++)
    {
        for ( int j = 0 ; j < y ;j++)
        {
            cin>>array[i] [j];
        }
    }

    multiplication(array1,array2,size1,size2);

    return 0;
}