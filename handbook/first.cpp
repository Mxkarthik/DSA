#include <bits/stdc++.h>
using namespace std;

int main ()
{
    ios::sync_with_stdio(false); // faster because it remove synocrinisation 
    cin.tie(nullptr);
    int m;

    if(cin>>m) {
        cout<< m << "\n"; //prefer n over endl
    }

    return 0;
}
