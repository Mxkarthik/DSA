#include <iostream>
using namespace std;


void print_top_triangle (int n)
{
    // the ASCII value of 'A' is 65
    for (int i = 65 ; i < )
}

void lower_triangle (int n)
{

}
int main ()
{
    //we have to print alphabhet diamond pattern for n = 5
    int n; cin>>n;

    // we have to print top triangle
    print_top_triangle(n);
    // we havt to print the lower trinagle
    lower_triangle(n);

    return 0;
}